import React from "react";
import Input from "../Input";
import Results from "../Results";

const Search = () => {
  return (
    <div>
      <Input />
      <Results />
    </div>
  );
};

export default Search;
